import RESTAdapter from '@ember-data/adapter/rest';

export default class ApplicationAdapter extends RESTAdapter {
  host = 'https://20jipe19g4.execute-api.us-east-1.amazonaws.com/dev';
  headers = {
    Authorization: 'R6euz~Y~m_3.!+*j@?{f',
  };
}
